{
  "name": "deer-isle-quest-site",
  "private": true,
  "type": "module",
  "version": "0.1.0",
  "scripts": {
    "predev": "node scripts/copy-data.mjs",
    "dev": "vite",
    "prebuild": "node scripts/copy-data.mjs",
    "build": "tsc -b && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "verify:domain": "tsx scripts/verify-domain.ts"
  },
  "devDependencies": {
    "tsx": "^4.19.2",
    "typescript": "^5.7.3",
    "vite": "^6.0.7",
    "vitest": "^4.1.11"
  },
  "dependencies": {
    "@fontsource-variable/recursive": "^5.1.1",
    "mermaid": "^11.4.1"
  }
}
